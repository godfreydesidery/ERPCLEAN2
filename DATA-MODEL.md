# Data Model

PostgreSQL 15+. Owned by solutions-architect. Each table: purpose, columns, keys, indexes,
constraints. Decisions behind the shape are in [docs/decisions/](docs/decisions/). This document
grows one module at a time; **IAM** is first.

## Conventions (apply to every table)
- `id BIGINT GENERATED BY DEFAULT AS IDENTITY PRIMARY KEY` — internal key, FK target.
- `uid VARCHAR(26) NOT NULL UNIQUE` — external ULID identifier on externally exposed entities;
  URLs address by uid. (Internal-only tables — `audit_log`, junctions — may omit uid.)
- Long ids serialise as JSON **strings** on the wire (global Jackson config).
- Timestamps `TIMESTAMPTZ`, stored UTC, displayed in branch/company time zone.
- Money/quantity `NUMERIC(18,4)` (none in IAM yet).
- Master status enum stored as `VARCHAR(32)`: `ACTIVE` | `INACTIVE` | `ARCHIVED`.
- Audit columns on mutable masters: `created_at`, `created_by`, `updated_at`, `updated_by`
  (`*_by` → `app_users.id`).
- Table names `snake_case`, **plural** for masters and owned-child tables (`companies`, `customers`,
  `app_users`, `product_barcodes`); **singular** for junctions/utility tables, named after both sides
  (`user_branch`, `role_permission`, `customer_branch`, `party_code_sequence`). FK constraints named
  `fk_<entity>_<ref>`, unique `uq_<entity>_<cols>` — using the **singular entity root** (`uq_customer_uid`
  on table `customers`); standalone indexes `ix_<table>_<cols>` use the **plural table name**
  (`ix_customers_company`). Columns are always singular (`company_id`, `price_list_id`).

---

# Module: IAM

Twelve tables. Hierarchy `organisation → company → branch`; identity `app_user`; access
`role`/`permission`/`role_permission`/`user_role`; assignment `user_branch`; company membership
`user_company`; session `refresh_token`; trail `audit_log`. Diagram (FK direction → parent):

```
organisation ─< company ─< branch
app_user ─< user_branch >─ branch
app_user ─< user_company >─ company
app_user ─< user_role >─ role ;  user_role >─ company ;  user_role >─ branch (nullable)
role ─< role_permission >─ permission
app_user ─< refresh_token
app_user ─< audit_log
```

## 1.1 `organisation`
Top of the tree; one row per deployment. Parent of all companies.

| Column | Type | Null | Notes |
|---|---|---|---|
| `id` | BIGINT identity | NO | PK |
| `uid` | VARCHAR(26) | NO | UNIQUE |
| `name` | VARCHAR(160) | NO | |
| `legal_name` | VARCHAR(200) | YES | |
| `default_time_zone` | VARCHAR(64) | NO | e.g. `Africa/Dar_es_Salaam`; companies/branches inherit |
| `status` | VARCHAR(32) | NO | |
| audit cols | | | created/updated by/at |

- `uq_organisation_uid (uid)`.

## 1.2 `company`
A legal entity within the organisation. Scoping parent of company-bound data (`company_id`).

| Column | Type | Null | Notes |
|---|---|---|---|
| `id` | BIGINT identity | NO | PK |
| `uid` | VARCHAR(26) | NO | UNIQUE |
| `organisation_id` | BIGINT | NO | FK → organisation.id |
| `code` | VARCHAR(20) | NO | UNIQUE within organisation |
| `name` | VARCHAR(160) | NO | |
| `legal_name` | VARCHAR(200) | YES | |
| `tax_id` | VARCHAR(60) | YES | TIN/VAT no. (locale set later) |
| `time_zone` | VARCHAR(64) | NO | inherits org, overridable |
| `status` | VARCHAR(32) | NO | |
| audit cols | | | |

- `fk_company_organisation (organisation_id)`; `uq_company_org_code (organisation_id, code)`;
  `ix_company_organisation (organisation_id)`.

## 1.3 `branch`
A physical location under a company. Smallest unit of stock & business day. (FR-IAM-03/04)

| Column | Type | Null | Notes |
|---|---|---|---|
| `id` | BIGINT identity | NO | PK |
| `uid` | VARCHAR(26) | NO | UNIQUE |
| `company_id` | BIGINT | NO | FK → company.id |
| `code` | VARCHAR(20) | NO | UNIQUE within company |
| `name` | VARCHAR(160) | NO | |
| `time_zone` | VARCHAR(64) | NO | inherits company, overridable |
| `is_default` | BOOLEAN | NO | DEFAULT false — company default branch (BR-2) |
| `status` | VARCHAR(32) | NO | |
| audit cols | | | |

- `fk_branch_company (company_id)`; `uq_branch_company_code (company_id, code)`;
  `ix_branch_company (company_id)`;
- **`uq_branch_company_default`** — `UNIQUE (company_id) WHERE is_default` → ≤1 default per company.

## 1.4 `app_user`
A login identity (≠ employee). Org-wide; username org-unique. May span companies. (FR-IAM-05, D5)

| Column | Type | Null | Notes |
|---|---|---|---|
| `id` | BIGINT identity | NO | PK |
| `uid` | VARCHAR(26) | NO | UNIQUE |
| `username` | VARCHAR(80) | NO | **UNIQUE org-wide** (BR-4); store lowercased |
| `password_hash` | VARCHAR(100) | NO | bcrypt cost ≥ 12 (FR-IAM-08) |
| `display_name` | VARCHAR(160) | NO | |
| `email` | VARCHAR(160) | YES | optional; for reset only (FR-IAM-05) |
| `phone` | VARCHAR(40) | YES | |
| `is_root` | BOOLEAN | NO | DEFAULT false — super-admin, bypasses scoping (FR-IAM-21) |
| `failed_login_count` | INT | NO | DEFAULT 0 (FR-IAM-09) |
| `locked_until` | TIMESTAMPTZ | YES | NULL = not locked |
| `last_login_at` | TIMESTAMPTZ | YES | |
| `password_changed_at`| TIMESTAMPTZ | YES | |
| `status` | VARCHAR(32) | NO | ACTIVE/INACTIVE/ARCHIVED — disabled ⇒ no login (US-IAM-005 AC3) |
| audit cols | | | |

- `uq_app_user_username (username)`; `uq_app_user_uid (uid)`.
- **No** `default_branch_id` / `default_company_id` column — default lives on `user_branch`
  (ADR-0001 D-B). Default company is *derived* from the default branch's company.

## 1.5 `permission`
Atomic access unit; org-wide catalogue; seeded via migration. (FR-IAM-11)

| Column | Type | Null | Notes |
|---|---|---|---|
| `id` | BIGINT identity | NO | PK |
| `code` | VARCHAR(80) | NO | UNIQUE; dot-separated, e.g. `USER.MANAGE`, `BRANCH.ASSIGN` |
| `module` | VARCHAR(40) | NO | UI grouping: `iam`, `sales`, … |
| `description` | VARCHAR(255) | NO | |

- `uq_permission_code (code)`; `ix_permission_module (module)`. No uid (internal reference data;
  addressed by `code`). Seeded set cannot be deleted (BR-7) — enforced in service.

## 1.6 `role`
Named, reusable permission bundle; org-wide. (FR-IAM-12)

| Column | Type | Null | Notes |
|---|---|---|---|
| `id` | BIGINT identity | NO | PK |
| `uid` | VARCHAR(26) | NO | UNIQUE |
| `code` | VARCHAR(40) | NO | UNIQUE; `CASHIER`, `BRANCH_MANAGER` |
| `name` | VARCHAR(120) | NO | |
| `description` | VARCHAR(255) | YES | |
| `is_system` | BOOLEAN | NO | DEFAULT false — system roles undeletable (BR-7) |
| `status` | VARCHAR(32) | NO | |
| audit cols | | | |

- `uq_role_code (code)`; `uq_role_uid (uid)`.

## 1.7 `role_permission`
Junction: role ⇄ permission. (FR-IAM-12)

| Column | Type | Null | Notes |
|---|---|---|---|
| `role_id` | BIGINT | NO | FK → role.id; part of composite PK |
| `permission_id` | BIGINT | NO | FK → permission.id; part of composite PK |

- PK `(role_id, permission_id)`; `fk_role_permission_role`, `fk_role_permission_permission`;
  `ix_role_permission_permission (permission_id)` (reverse lookup).

## 1.8 `user_role`
Role assignment: binds a role to a user, scoped to a company, optionally one branch. (FR-IAM-13/14)

| Column | Type | Null | Notes |
|---|---|---|---|
| `id` | BIGINT identity | NO | PK |
| `uid` | VARCHAR(26) | NO | UNIQUE |
| `user_id` | BIGINT | NO | FK → app_user.id |
| `role_id` | BIGINT | NO | FK → role.id |
| `company_id` | BIGINT | NO | FK → company.id — the scope |
| `branch_id` | BIGINT | YES | FK → branch.id; **NULL = all branches in the company** (FR-IAM-13) |
| `granted_at` | TIMESTAMPTZ | NO | |
| `granted_by` | BIGINT | NO | FK → app_user.id |
| `revoked_at` | TIMESTAMPTZ | YES | NULL = active |

- FKs as above; `ix_user_role_user (user_id)`, `ix_user_role_lookup (user_id, company_id, branch_id)
  WHERE revoked_at IS NULL` (the resolve-per-scope hot path, ADR-0001 D-E).
- `uq_user_role_active` — `UNIQUE (user_id, role_id, company_id, branch_id) WHERE revoked_at IS NULL`
  → no duplicate active grant. (NULL branch_id participates; Postgres treats NULLs as distinct, so the
  service also guards the company-wide duplicate case.)
- **BR-5** (branch belongs to the assignment's company) enforced in service (cross-column FK not
  expressible as a simple constraint).

## 1.9 `user_branch`
Branch assignment: a user assigned to many branches, exactly one default. Decoupled from roles —
a user may be "present" without a role (FR-IAM-15..19, FR-IAM-24).

| Column | Type | Null | Notes |
|---|---|---|---|
| `id` | BIGINT identity | NO | PK |
| `uid` | VARCHAR(26) | NO | UNIQUE |
| `user_id` | BIGINT | NO | FK → app_user.id |
| `branch_id` | BIGINT | NO | FK → branch.id (company reachable via branch) |
| `is_default` | BOOLEAN | NO | DEFAULT false — the user's single default branch |
| `assigned_at` | TIMESTAMPTZ | NO | drives earliest-assigned fallback (ADR-0001 D-D) |
| `assigned_by` | BIGINT | NO | FK → app_user.id |

- `fk_user_branch_user`, `fk_user_branch_branch`;
- `uq_user_branch_user_branch` — `UNIQUE (user_id, branch_id)` → no double-assignment;
- **`uq_user_branch_default`** — `UNIQUE (user_id) WHERE is_default` → ≤1 default per user (BR-1);
- `ix_user_branch_user (user_id)`, `ix_user_branch_branch (branch_id)`.
- **FR-IAM-17** (default ∈ assignments) is automatic: `is_default` lives **on** the assignment row,
  so a default that isn't an assignment is unrepresentable.

## 1.10 `refresh_token`
Hashed, single-use, rotated. (FR-IAM-06)

| Column | Type | Null | Notes |
|---|---|---|---|
| `id` | BIGINT identity | NO | PK |
| `user_id` | BIGINT | NO | FK → app_user.id |
| `token_hash` | VARCHAR(64) | NO | UNIQUE; SHA-256 of the opaque token (never store raw) |
| `issued_at` | TIMESTAMPTZ | NO | |
| `expires_at` | TIMESTAMPTZ | NO | issued_at + 7d |
| `rotated_at` | TIMESTAMPTZ | YES | set when used to mint a successor (then invalid) |
| `revoked_at` | TIMESTAMPTZ | YES | set on logout |
| `replaced_by_id` | BIGINT | YES | FK → refresh_token.id (rotation chain, reuse detection) |
| `client_company_id`| BIGINT | YES | scope active at issue (informational) |
| `client_branch_id` | BIGINT | YES | scope active at issue (informational) |

- `uq_refresh_token_hash (token_hash)`; `ix_refresh_token_user (user_id)`;
  `ix_refresh_token_expiry (expires_at)` (cleanup). A presented token with `rotated_at`/`revoked_at`
  set ⇒ reuse → revoke the whole chain (US-IAM-002 AC2).

## 1.11 `audit_log`
Append-only IAM (and later, global) action trail. Internal — no uid. (FR-IAM-23, US-IAM-010)

| Column | Type | Null | Notes |
|---|---|---|---|
| `id` | BIGINT identity | NO | PK |
| `actor_user_id` | BIGINT | YES | FK → app_user.id (NULL for system/bootstrap) |
| `action` | VARCHAR(80) | NO | `USER.CREATE`, `ROLE.GRANT`, `BRANCH.ASSIGN`, `LOGIN.FAIL`, … |
| `target_type` | VARCHAR(60) | NO | entity name, e.g. `app_user`, `user_branch` |
| `target_id` | BIGINT | YES | target row id |
| `target_uid` | VARCHAR(26) | YES | target uid where applicable |
| `company_id` | BIGINT | YES | context |
| `branch_id` | BIGINT | YES | context |
| `detail` | JSONB | YES | before/after or extra context (Postgres-native, ADR uses JSONB) |
| `at` | TIMESTAMPTZ | NO | event time |
| `ip` | VARCHAR(45) | YES | request IP (login events) |

- `ix_audit_log_target (target_type, target_id, at)`; `ix_audit_log_actor (actor_user_id, at)`;
  `ix_audit_log_action_at (action, at)`. **No UPDATE/DELETE** through the app (append-only, US-IAM-010
  AC2) — enforced by the audit aspect + a DB role without UPDATE/DELETE grant on this table at deploy.

## 1.12 `user_company`
Explicit user ⇄ company membership — the **authoritative write-path prerequisite** for tenancy
(ADR-0046, supersedes the non-authoritative phase of ADR-0045). Added in V77; no schema change since.

| Column | Type | Null | Notes |
|---|---|---|---|
| `id` | BIGINT identity | NO | PK |
| `uid` | VARCHAR(26) | NO | UNIQUE; assigned by `@PrePersist`, never seeded in SQL |
| `user_id` | BIGINT | NO | FK → app_user.id |
| `company_id` | BIGINT | NO | FK → company.id |
| `assigned_at` | TIMESTAMPTZ | NO | DEFAULT now() |
| `assigned_by` | BIGINT | YES | FK → app_user.id |
| `revoked_at` | TIMESTAMPTZ | YES | NULL = active (mirrors `user_role`) |
| `version` | BIGINT | NO | DEFAULT 0 — optimistic lock |

- `uq_user_company_uid (uid)`; `uq_user_company_active` — `UNIQUE (user_id, company_id) WHERE
  revoked_at IS NULL` → ≤1 active membership per (user, company);
  `ix_user_company_company (company_id)`, `ix_user_company_user (user_id)`.
- **Authoritative on the write path:** `UserRoleService.grant` and `UserBranchService.assign` require
  an active membership for the target company (else **409**); the earlier auto-create on grant/assign
  is removed. Membership is created only via the explicit `POST /api/v1/user-companies`
  (`USER.COMPANY.MANAGE`).
- **Removal is blocked (no cascade)** while the user still holds any active role grant or branch
  assignment in that company — access must be cleared first.
- **Read/isolation oracle stays additive** (defensive superset): a user is "in" company C if they
  hold an active `user_role` **or** `user_branch` **or** `user_company` in C. After the write-path
  gate, every active role/branch implies a membership row, so additive ≡ `user_company`-only — but
  keeping it additive leaves the (separately hardened) isolation reads unchanged. **Root bypasses
  tenant scope but NOT the membership gate.**
- **BR-6 re-decided (ADR-0046):** branch and role assignment remain independent of each other, but
  each now requires prior company membership.
- Coverage is guaranteed by an idempotent every-boot reconcile (`UserCompanyBackfill`) — **no Flyway
  data migration**; bootstrap/seeders persist `user_role`/`user_branch` directly and bypass the gate.

---

## Tenant-isolation discipline (write + read)
Company isolation is enforced in the **service layer** (no schema change). The authoritative rule,
reaffirmed by the 2026-06-26 security audit (28 cross-company leaks across 11 modules, all fixed):
**derive the scope-company from the LOADED entity, never from a caller-supplied parameter, and apply
the same guard to reads AND writes.** Two bug classes the audit closed: (a) *confused-deputy via a
secondary identifier* — scope-checking a caller-supplied `companyId` while loading by a separate,
unverified `id`/`uid`; (b) *missing write-scope* — uid-addressed mutators that load by uid without
re-checking the loaded entity's company (writing a record you cannot read). Fixes use company-scoped
repository finders + ownership re-checks. A regression guard (`TenantScopingRulesTest`, an ArchUnit
`FreezingArchRule`) fails the build if a `..service..` class makes a bare `findById` /
`getReferenceById` on a Spring Data repository, with ~197 pre-existing calls frozen as a baseline.

---

## Seed data (Flyway)
- **Permissions** (org-wide catalogue, module `iam`): `USER.MANAGE`, `USER.VIEW`, `ROLE.MANAGE`,
  `ROLE.VIEW`, `PERMISSION.VIEW`, `BRANCH.MANAGE`, `BRANCH.ASSIGN`, `COMPANY.MANAGE`,
  `COMPANY.VIEW`, `USER.COMPANY.MANAGE` (explicit company-membership assign/remove), `AUDIT.VIEW`.
  (Final list confirmed with backend-engineer per endpoint.)
- **System roles** (`is_system = true`): `SUPER_ADMIN` is *not* a role — root is the `app_user.is_root`
  flag (ADR-0001). Seed `ORG_ADMIN` (all IAM permissions) and the obvious operational roles
  (`BRANCH_MANAGER`, etc.) as requirements for later modules firm up.
- **Bootstrap** (FR-IAM-22): on empty DB, create organisation + first company + that company's
  default branch (`is_default = true`) + root `app_user` (`is_root = true`) from env. Idempotent —
  skipped if any organisation exists (US-IAM-009 AC3).

## Requirement → schema coverage
| Requirement | Realised by |
|---|---|
| FR-IAM-01..04 org/company/branch + defaults | §1.1–1.3, `uq_branch_company_default` |
| FR-IAM-05 username login, email optional | §1.4 `uq_app_user_username` |
| FR-IAM-06/07 tokens, rotation, revoke | §1.10 `refresh_token` |
| FR-IAM-08/09 password, lockout | §1.4 `password_hash`, `failed_login_count`, `locked_until` |
| FR-IAM-11/12 permission/role org-wide | §1.5–1.7 |
| FR-IAM-13/14 role assignment company/branch-scoped | §1.8 `user_role` |
| FR-IAM-15..19 many branches, one default, fallback | §1.9 `user_branch`, `uq_user_branch_default`, `assigned_at` |
| FR-IAM-20/21 both-required, super-admin | service checks + `app_user.is_root` |
| FR-IAM-22 bootstrap | seed section |
| FR-IAM-23 audit | §1.11 `audit_log` |
| FR-IAM-24 assignment decoupled from roles | §1.9 (no role FK on `user_branch`) |
| BR-1 one default/user | `uq_user_branch_default` |
| BR-2 one default/company | `uq_branch_company_default` |
| BR-3 default ∈ assignments | structural (flag on assignment row) |
| BR-4 username org-unique | `uq_app_user_username` |
| BR-6 company membership prerequisite for role/branch | §1.12 `user_company` + service gate (ADR-0046) |
| BR-7 system roles/perms undeletable | `is_system` / seeded + service guard |
