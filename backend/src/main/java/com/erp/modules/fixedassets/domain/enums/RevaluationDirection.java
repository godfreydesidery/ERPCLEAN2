package com.erp.modules.fixedassets.domain.enums;

/** Direction of a revaluation — upward (DR asset / CR reserve) or downward (DR loss / CR asset) (ADR-0030 D-3). */
public enum RevaluationDirection {
    UP,
    DOWN
}
