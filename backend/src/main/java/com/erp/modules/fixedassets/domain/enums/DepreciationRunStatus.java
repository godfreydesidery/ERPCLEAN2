package com.erp.modules.fixedassets.domain.enums;

/**
 * Status of a depreciation run (ADR-0030 D-3).
 * Single value for forward-compat — a future DRAFT state is additive.
 */
public enum DepreciationRunStatus {
    POSTED
}
