package com.erp.modules.crm.domain.enums;

/** Coarse origin of a lead (ADR-0031 D-2). Full campaign object deferred §2. */
public enum LeadSource {
    WEBSITE,
    REFERRAL,
    WALK_IN,
    CAMPAIGN,
    COLD_CALL,
    EXISTING_CUSTOMER,
    OTHER
}
