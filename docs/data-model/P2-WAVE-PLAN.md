# P2 Wave Plan (81 gaps)

Sequencing for the P2 tranche of [GAP-FIX-WORKLIST.md](GAP-FIX-WORKLIST.md) (lines 212–398). Principle: **mechanical-first** (fast, low-risk, additive nullable columns / enum swaps / entity-field sync — batched by module cluster to share one migration), **then design-themes** (grouped by the decision they share; several are now unblocked by P1). Each wave: new additive migration(s), entity/DTO/mapper sync, compile+unit, full IT verify, commit, merge to develop.

**Before adding anything, verify it isn't already shipped** — some P2 rows were closed by P1 mechanical quick-wins (e.g. X11 Product tracking-flag sync `f18a237`; PartyBase/address `country` partly via D-3). Skip and note duplicates.

## Mechanical waves (≈45 gaps)

- **P2-M1 — Finance core (gl/tax/fx).** CoA `currency`; FiscalYear `reopenedAt/By`; JournalBatch control totals; JournalEntry `reversedByEntryId`/`reversed` + header currency/totals; JournalLine `taxCode/taxAmount`; VatReturn payment tracking + turnover figures; WhtTransaction `ratePct`; Currency `numericCode`; FxRevaluationRun `executedBy`/`reversalDate`/`rateType`; FxRevaluationRunLine `priorRate`/GL-line linkage.
- **P2-M2 — AP/cashbank.** SupplierBill `taxPointDate`/`receivedDate`; SupplierBillLine line dimensions (costCentre/department); ApPaymentAllocation `allocatedAt/By`; ApDebitNote `origin` enum; CashBankAccount `iban/swift/bic/sortCode` + `overdraftLimit/minimumBalance/lastReconciledDate/Balance`; CashTransaction `valueDate`/`chequeId`.
- **P2-M3 — Sales/products.** Quotation `customerPoNumber/revisionNo/probability`; SalesOrder `customerPoNumber/requestedDeliveryDate/promisedDate`; SalesOrderLine `requestedDate/discountReason`; SalesInvoice `customerPoNumber`; TaxRate `name/taxType`; StandingOrder recurrence bookkeeping; ProductBarcode `barcodeType`/uom; ProductBulkPack flags; PriceTier `maxQty`; ProductPrice `effectiveFrom/To`; Product `brand/manufacturer/weight/volume/dimensions/hsCode` + tracking-flag sync (X11, verify).
- **P2-M4 — Purchases.** PurchaseOrder `buyerId`/`invoicedAmount`; PurchaseOrderLine `billedQtyInBase/requiredByDate/cancelledQty`; PurchaseRequisition `priority`/`preferredSupplierId`; PurchaseRequisitionLine `requiredByDate/suggestedSupplierId/convertedToPoLineUid`; RfqLine `requiredByDate/specification`; RfqSupplier response tracking.
- **P2-M5 — HR/stock/manufacturing.** EmployeeLoanInstallment status enum + `deductedAmount/dueDate/paidAt`; StatutoryRateSet `basis` enum + `floorAmount`; PayrollLine `contractId`; PayrollLineItem `itemKind` enum + taxable/pensionable snapshot; StockTransfer/StockCount enum swaps (X22); StockOnHand `maxQty/lastMovementAt/lastCountedAt`; StockTransfer `dispatchedBy/receivedBy/expectedArrivalDate`; WorkOrder `targetLocationId`; WorkOrderComponent `returnedQty/scrapQty/unitId`.
- **P2-M6 — CRM/projects/fixedassets/approvals/iam/parties.** CRM Lead/Opportunity/Activity scalars + `lossReason` enum; Project/ProjectTask/ProjectTimesheet dates+actuals+assignee; FixedAsset `serialNumber/model/manufacturer/barcode`; ApprovalPolicy effective dates; ApprovalRequest `currentStepSequence`; Company `vrn/logoRef/fiscalYearStartMonth`; Branch `managerId/branchType`; AppUser `mustChangePassword/passwordExpiresAt/lastLoginIp/employeeId`; RefreshToken device cols; X30 audit-column entity exposure; PartyBase `country`; Customer `taxExempt/defaultCurrency`.

## Design waves (≈36 gaps)

- **P2-D1 — Document payment-terms + settlement discount** (unblocked by D-2 master; = ADR-0040 D-2 Phase 2). `paymentTermsId` on Quotation/SO/SI/PO/SupplierBill; ArInvoice settlement discount (discountDueDate/Amount); ArReceiptAllocation/ApPaymentAllocation `discountAmount/writeOffAmount`; SalesInvoice/SupplierBill `dueDate` derivation.
- **P2-D2 — Sales addresses + snapshots** (unblocked by D-3; = Wave-4 follow-up). shipTo/billTo on SO/SI/Delivery + snapshot population; PartyBase address polish.
- **P2-D3 — Instrument links + receipt/payment lifecycle** (ties to D-9 cheque). ArReceipt `tenderType` enum + instrument link + bounce/reversal; ApPayment status/`paymentRunId`/tender; ApDebitNote outstanding/applied/fx/status; SalesInvoicePayment instrument; CashTransfer fee leg; Cheque bounce/book/stale; BankReconciliation outstanding/adjustment journal.
- **P2-D4 — Dimension framework everywhere.** CoA dimension-requirement flags; PO/PO-line + AP-bill-line dimensions; poster enforcement.
- **P2-D5 — Master-data expansions.** Customer priceList/agent/territory/segment; Supplier whtType/currency/leadTime/minOrderValue; Agent territory/quota; UoM dimension/fractional; PriceList currency/effective/scope/isDefault; Promotion targeting/usage.
- **P2-D6 — HR org + policy.** Employee termination/manager/position/grade; Department hierarchy + costCentre; EmploymentContract terms; EmployeeLoan interest/schedule; LeaveType/LeaveBalance carry-forward/accrual; PayComponent statutory/formula; PayeBandSet resident scope.
- **P2-D7 — Remaining design.** GL FS-mapping/reportingGroup, FiscalPeriod module soft-close, GlConfig effective dating, JournalEntry DRAFT lifecycle, JournalLine subledger key; ArWriteOff/AssetDisposal/AssetRevaluation approvals + provenance; FixedAsset componentisation/insurance; BillMatch split/matchType/variance; VatReturn amendment/penalty; CurrencyRate effectiveTo; FxRevaluationRunLine drill-down; budgeting type/branch/quantity; StockLocation hierarchy + StockTransferLine partial + StockCount workflow; on-order/ATP (X20); manufacturing BOM/routing/lot (X16/X19); PurchaseSettings; Rfq award/SupplierQuote scoring; PosSession per-tender; SalesReturn handling; Delivery logistics/POD; IAM Org/Company/Branch contact-address + Org subscription + AppUser MFA.

## Status
- [x] **P2-M1** (gl/tax/fx) — shipped `170498c`, green.
- [x] **P2-M2…M6** (ap/cashbank, sales/products, purchases, hr/stock/mfg, crm/projects/fixedassets, approvals/iam/parties) — ~148 columns/enums, built by 6 parallel agents, green (970 tests).
- [ ] P2-D1 … P2-D7 (design)

## Mechanical follow-up tranche (deferred from M2–M6, all low-risk)
Columns/enums landed; these remaining bits were deferred to stay strictly mechanical / avoid cross-module breakage:
- **Cross-module enum-swaps kept as String** (compile-safe): `ap_debit_notes.origin` → `ApDebitNoteOrigin` (enum TYPE + `origin_ref` added; mapping needs purchases caller `RaiseDebitNoteRequest.origin` + `PurchaseReturnServiceImpl` split of `KIND:{uid}`); `payroll_line_items.item_kind` → enum (read as String by gl `PayrollPostingHandler`).
- **Population wiring** (snapshot/derived columns added but not yet written by services): control/snapshot cols (loan `deducted_amount/due_date/paid_at`, `contract_id`, taxable/pensionable, stock `max_qty/last_movement_at/last_counted_at`, WO `target_location_id`), `opportunities.stage_changed_at` stamp in `advanceStage`, PO `billing_status` roll-up.
- **DTO symmetry**: `SupplierDto/AgentDto/OtherPartyDto.country`; `RfqSupplier` has no read DTO; Customer create/update request DTOs not extended (country/taxExempt/defaultCurrency are entity-settable + read-exposed only); `ProductDto.category` pre-existing omission.
- **Soft-FK → real FK** (optional, via later ALTER once referenced tables exist in migration order): `cash_transactions.cheque_id`, `supplier_bill_lines` dimension cols, `product_barcodes.uom_id`, `project_tasks.assignee_user_id`.
